export interface IBusines {
    id: number,
    name: string,
    description: string,
    adress: string,
    web: string
    category: string,
    notation: number,
    image: string,
    slug: string
}